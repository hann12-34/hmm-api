# Copy to .env locally. On Render: set these in the NEW hmm-api service only.
PORT=3001
MONGODB_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/hmm_maintenance?retryWrites=true&w=majority
JWT_SECRET=change-me-to-a-long-random-string
CORS_ORIGIN=*
